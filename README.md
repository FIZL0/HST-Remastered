# HST (SCUS-97610) Upscale

Drop the contents of `replacements/` into your texture replacement folder.

e.g. `/home/[user]/.config/PCSX2/textures/SCUS-97610/replacements`

Enable texture replacements in PCSX2 settings.

These were upscaled with the 4xHDcube4 model.
